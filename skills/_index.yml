generated: '2026-07-19'
method: searched
source: https://github.com/Lightning-AI/skills
notes: >-
  Lightning AI publishes its own Agent Skills in the SKILL.md format at
  https://github.com/Lightning-AI/skills. Each file below is saved VERBATIM from that repository
  (renamed to the <slug>-<skill>.md convention); nothing was generated by API Evangelist. Install
  upstream with `npx skills add Lightning-AI/skills`. All skills are built around the
  `lightning-sdk` Python package and its `lightning` CLI (runnable via uvx), with
  `lightning api <endpoint>` as the raw REST escape hatch.
upstream:
  repository: https://github.com/Lightning-AI/skills
  authoring_guide: https://github.com/Lightning-AI/skills/blob/main/AUTHORING.md
  install: npx skills add Lightning-AI/skills
  format: https://code.claude.com/docs/en/skills
prerequisites:
- uv installed (skills invoke the CLI via uvx lightning-sdk)
- A Lightning AI account — `lightning login` or LIGHTNING_API_KEY + LIGHTNING_USER_ID
skills:
- file: lightningai-studios.md
  name: lightning-studios
  upstream: https://github.com/Lightning-AI/skills/blob/main/lightning-studios/SKILL.md
  summary: >-
    Create, start, stop and manage cloud GPU Studios; switch machine types, run commands,
    transfer files, SSH in.
  commands: [lightning studio create, lightning studio start, lightning studio stop, lightning studio ssh]
- file: lightningai-jobs.md
  name: lightning-jobs
  upstream: https://github.com/Lightning-AI/skills/blob/main/lightning-jobs/SKILL.md
  summary: >-
    Launch and monitor batch jobs (single and multi-machine) on CPUs/GPUs, stream logs, collect
    artifacts.
  commands: [lightning job run, lightning job list, lightning job stop, lightning mmt run]
- file: lightningai-deployments.md
  name: lightning-deployments
  upstream: https://github.com/Lightning-AI/skills/blob/main/lightning-deployments/SKILL.md
  summary: >-
    Deploy containers or HuggingFace models behind autoscaled HTTPS endpoints; manage replicas,
    releases, endpoint auth and logs.
  commands: [lightning deployment create, lightning deployment update, lightning deployment logs]
- file: lightningai-sandboxes.md
  name: lightning-sandboxes
  upstream: https://github.com/Lightning-AI/skills/blob/main/lightning-sandboxes/SKILL.md
  summary: >-
    Fast ephemeral isolated VMs for safe code execution — run commands, background processes,
    file I/O, snapshots, PTY sessions.
  commands: [lightning sandbox create, lightning sandbox run, lightning sandbox snapshot]
- file: lightningai-llm-gateway.md
  name: lightning-llm-gateway
  upstream: https://github.com/Lightning-AI/skills/blob/main/lightning-llm-gateway/SKILL.md
  summary: >-
    Call hosted LLMs (OpenAI, Anthropic, Google, open-weights) through the Lightning Model APIs
    gateway; chat, streaming, multi-turn, images, model metadata, plus the checkpoint registry.
  commands: [lightning api-key get, lightning model upload, lightning model download]
- file: lightningai-artifacts.md
  name: lightning-artifacts
  upstream: https://github.com/Lightning-AI/skills/blob/main/lightning-artifacts/SKILL.md
  summary: >-
    Publish a file and get a durable, public lightning.ai/artifacts/<id> link that never expires
    and renders inline; list, revoke and delete shares via the CLI.
related:
- cli/lightningai-cli.yml
- authentication/lightningai-authentication.yml
